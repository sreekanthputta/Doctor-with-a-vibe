# Server-only provider credentials. Never prefix these with VITE_ or expose them to browser code.
DEEPGRAM_API_KEY=
MEDPLUM_CLIENT_ID=
MEDPLUM_CLIENT_SECRET=
MEDPLUM_BASE_URL=https://api.medplum.com
STEDI_API_KEY=

# Roadmap-only; unused by the administrative MVP.
MOSS_PROJECT_ID=
MOSS_PROJECT_KEY=

# Optional live conformance gates. Fixture tests remain the default.
RUN_LIVE_MEDPLUM_TESTS=false
RUN_LIVE_DEEPGRAM_TESTS=false
RUN_LIVE_STEDI_TESTS=false

# Test/development-only reset. Never enable in production.
ENABLE_DEMO_RESET=false
DEMO_RESET_TOKEN=
